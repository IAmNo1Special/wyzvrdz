# Example: Removing a Task

**User Intent:** "Cancel the morning briefing job."

**Reasoning:** I need to identify the job by its unique name and use the remove action.

**Execution:**

```bash
python scripts/manage_cron.py --action remove --name "Morning Briefing"
```
